# api

Placeholder for the backend service. It is a workspace so the monorepo layout is stable; the server, its
storage configuration and its dependency on the shapes package are added when the backend is built.
